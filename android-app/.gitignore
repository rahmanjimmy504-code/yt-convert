# Dependencies
node_modules/

# Web build output (regenerated by `npm run build`, copied into the APK by
# `npx cap sync android`)
dist/

# Android build output
android/.gradle/
android/build/
android/app/build/
android/captures/
android/local.properties
android/.idea/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
*.apk
*.aab

# Signing material — NEVER commit these (build plan step 9)
*.jks
*.keystore
keystore.properties

# Editors and OS
.DS_Store
.vscode/
.idea/
*.iml
*.log
